{
  "name": "harness",
  "version": "1.12.0",
  "description": "pi extension: /run autonomous task harness — baseline, declared scope, gate-verified edits, telemetry report, crash recovery",
  "keywords": ["pi", "pi-extension", "extension", "harness", "run", "automation", "agent"],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test core/harness-core.test.mjs",
    "validate-skills": "node core/compile-skills.mjs"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.2",
    "@types/node": "^22",
    "typescript": "^5.9.3"
  }
}